```Swift
// 두 자연수를 입력받아 공백을 기준으로 나눠배열로 반환하고 map으로 Int로 형변환해준다
let a = readLine()!.split{$0==" "}.map{Int($0)!}

// 사칙연산해서 출력한다.
print("\(a[0]+a[1])\n\(a[0]-a[1])\n\(a[0]*a[1])\n\(a[0]/a[1])\n\(a[0]%a[1])")
```


